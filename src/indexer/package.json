{
  "name": "xpulse-indexer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "ponder dev",
    "start": "ponder start",
    "build": "ponder build",
    "codegen": "ponder codegen"
  },
  "dependencies": {
    "@ponder/core": "^0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
